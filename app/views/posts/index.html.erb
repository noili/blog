<h1>Posts#index</h1>
<%@posts.each do |post|%>
  <h1><%=post.title.titleize%></h1>
  <p><%=post.body%></h1></p>
  <%=link_to('Show', post_path(post))%>
  <%=link_to('edit', edit_post_path(post))%>
<%end%>
